
spring:
  application:
    mode: ${APP_MODE:dev}
    version: ${APP_VERSION:v1}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
      readinessstate:
        enabled: true