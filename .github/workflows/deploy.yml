name: Deploy to Minikube via Helmfile (NodePort only)

on:
  push:
    branches:
      - master    # master 브랜치 push 시 자동 배포

jobs:
  deploy:
    runs-on: windows-latest    # ✅ Windows 환경 지정

    steps:
      # ✅ 저장소 체크아웃
      - name: Checkout Repository
        uses: actions/checkout@v4

      # ✅ Helm & Helmfile 설치 확인 (없으면 설치)
      - name: Ensure Helm and Helmfile Installed
        shell: pwsh
        run: |
          # Helm 확인
          if (-not (Get-Command helm -ErrorAction SilentlyContinue)) {
            Write-Host "🛠️ Helm not found. Installing via Chocolatey..."
            choco install kubernetes-helm -y
          } else {
            Write-Host "✅ Helm already installed: $(helm version --short)"
          }

          # Helmfile 확인
          if (Get-Command helmfile -ErrorAction SilentlyContinue) {
              Write-Host "✅ Helmfile present: $(helmfile --version)"
          } else {
            Write-Host "ERROR: Helmfile not installed or cant find"
          }

      # ✅ Helmfile sync 실행 (dev 환경)
      - name: Deploy Helmfile
        shell: pwsh
        run: |
          helmfile -e dev sync
          Write-Host "✅ Helmfile sync completed successfully."

      # ✅ NodePort 서비스 확인
      - name: Show NodePort Info
        shell: pwsh
        run: |
          kubectl get svc -n dev
          Write-Host "✅ Copy the NodePort from above and open: http://localhost:<nodeport>"
